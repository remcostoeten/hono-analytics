{
  "name": "hono-analytics",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "sdk",
    "example"
  ],
  "scripts": {
    "dev": "./dev.sh",
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
