{
  "name": "honolytics",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter docs dev",
    "build": "bun run --filter honolytics build",
    "build:all": "bun run build && bun run --filter docs build"
  },
  "dependencies": {
    "ci-info": "^4.3.0",
    "create-fumadocs-app": "^15.8.3"
  }
}
